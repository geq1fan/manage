/**
 * 查看历史记录详情
 * @param title
 * @param url
 * @param w
 * @param h
 */
function status_view(title, url, w, h) {
    layer_show(title, url, w, h);
}

/**
 * 设置生产参数
 * @param title
 * @param url
 * @param w
 * @param h
 */
function productline_set(title, url, w, h) {
    layer_show(title, url, w, h);
}